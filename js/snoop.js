var SNOOP = {};

SNOOP.createDiv = function () {

    // create a div with a specified attributes an
    //

};

SNOOP.findDiff = function (matchedBoard) {

    var previousBoard = null;
    // compares 2 arrays of objects
    // finds the difference
    // returns the index

    return function () {

        //


        previousBoard = matchedBoard;
        return matchedBoard;
    }
}

module.exports = SNOOP; 
